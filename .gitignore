# GitLab token (never commit)
glab/token
glab/hamgit-token

# Local config override (real host/group values stay out of git)
ansible/group_vars/all.yml

# Generated clone list
glab/repos.txt

# Working trees and reports
ansible/workspace/*
!ansible/workspace/.keep
ansible/reports/*
!ansible/reports/.keep

# Ansible
*.retry
ansible/.ansible-tmp/

# Editors / OS
.DS_Store
.vscode/
.idea/
