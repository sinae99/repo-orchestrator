# actions/

tools that run on scanned files. full explanation (catalogue, params, how to
write one) → [`docs/README.md`](../../docs/README.md)

```bash
cp -r ansible/actions/_template ansible/actions/my-action
./reporker validate my-action
```

ai agents: follow [`AGENTS.md`](../../AGENTS.md).
